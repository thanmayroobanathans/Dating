# VIT PULSE V7

Upgrade of V6: Google auth, Turing UI, 36-input multidimensional matching, explicit precise browser GPS permission, Firestore result persistence, private per-user rules and anonymous aggregation.

1. Paste your Firebase Web App config into firebase-config.js.
2. Enable Google Authentication.
3. Add thanmayroobanathans.github.io to Authorized domains.
4. Create Firestore and deploy firestore.rules.
5. Deploy to GitHub Pages over HTTPS.
